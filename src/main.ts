import { createApp, defineComponent, h, ref, reactive, onMounted } from 'vue'
// import App from './App.vue'
import HelloWorld from '@/components/HelloWorld.vue'
import App from './App';

createApp(App).mount('#app')
